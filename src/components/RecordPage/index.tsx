import React from 'react';

type RecordPageProps = {};

function RecordPage(props: RecordPageProps) {
  return (
    <div>
      {/* TODO: 여기에 화면에 가득 찬 녹화 화면 */}
      {/* 추가로 옵션을 선택할 수 있도록? */}
    </div>
  );
}

RecordPage.defaultProps = {
};

export default RecordPage;